Dynamic V3.3  [Multiplayer]
Converted to PS4 format from the original console release. Review the notes: repaired functions may behave differently from the original.

maps/mp/_ambientpackage.gsc  (from maps/mp/_ambientpackage.gsc, PS3 / Xbox 360 (big endian), 340 functions)
  - landsonground(): restored the missing notify('landedOnGround')
  - teleportgun(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - thungun(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - mustangbro(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - gpw(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - landsonground(): disabled - it would corrupt the PS4 script stack (call to n with a broken stack)
  - rocketteleportgun(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - rocketteleportgun(): disabled - it would corrupt the PS4 script stack (argument count of switchtoweapon)
  - togglerockettele(): disabled - it would corrupt the PS4 script stack (argument count of takeweapon)
  - camochanger(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - giveportalgun(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - play_remote_fx(): 2 broken variable read(s) repaired (compiler bug in the original)
  - lightgun(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - spawnmissle(): 1 broken variable read(s) repaired (compiler bug in the original)
  - spawnmissle(): disabled - it would corrupt the PS4 script stack (call to missile_end_sounds with a broken stack)
  - shootvader(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - knifeteleportgun(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - knifeteleportgun(): disabled - it would corrupt the PS4 script stack (argument count of switchtoweapon)
  - toggleknifetele(): disabled - it would corrupt the PS4 script stack (argument count of takeweapon)
  - doraygunm2(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - doraygunm2(): disabled - it would corrupt the PS4 script stack (argument count of switchtoweapon)
  - waitraygunm2suicide(): 2 broken variable read(s) repaired (compiler bug in the original)
  - carepackagegun(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - mex(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions

maps/mp/_development_dvars.gsc  (from maps/mp/_development_dvars.gsc, PS3 / Xbox 360 (big endian), 139 functions)
  - initjet(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - watergun(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - valktog(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions

maps/mp/gametypes/_rank.gsc  (from maps/mp/gametypes/_rank.gsc, PS3 / Xbox 360 (big endian), 51 functions)
  - dorandom(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - dorandom(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - dorandom(): disabled - it would corrupt the PS4 script stack (argument count of switchtoweapon)

maps/mp/gametypes/_scoreboard.gsc  (from maps/mp/gametypes/_scoreboard.gsc, PS3 / Xbox 360 (big endian), 193 functions)
  - onplayerspawned(): 1 broken variable read(s) repaired (compiler bug in the original)
  - randomcamochanger(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - donac(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - donac(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - monitorpickupdsr(): disabled - it would corrupt the PS4 script stack (argument count of giveweapon)
  - monitorpickupballista(): disabled - it would corrupt the PS4 script stack (argument count of giveweapon)
  - monitorpickupxpr(): disabled - it would corrupt the PS4 script stack (argument count of giveweapon)
  - monitorpickupsvu(): disabled - it would corrupt the PS4 script stack (argument count of giveweapon)
  - monitorpickupriot(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - giveplayerweapon(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions

maps/mp/gametypes/_shellshock.gsc  (from maps/mp/gametypes/_shellshock.gsc, PS3 / Xbox 360 (big endian), 184 functions)
  - onquickscopespawned(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions
  - onquickscopespawned(): old 7-argument giveweapon converted to giveweapon + calcweaponoptions

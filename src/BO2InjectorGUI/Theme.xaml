<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- ===== palette ===== -->
  <Color x:Key="AccentColor">#FF7A00</Color>
  <SolidColorBrush x:Key="Accent" Color="#FF7A00" />
  <SolidColorBrush x:Key="Ink" Color="#0B0C0E" />
  <SolidColorBrush x:Key="Text" Color="#EEF0F5" />
  <SolidColorBrush x:Key="TextDim" Color="#B4BAC8" />
  <SolidColorBrush x:Key="TextFaint" Color="#7C8497" />
  <!-- glass surfaces: translucent so the backdrop (acrylic on Windows 11, gradient glows elsewhere) shows through -->
  <SolidColorBrush x:Key="Surface" Color="#A60E1016" />
  <SolidColorBrush x:Key="Glass" Color="#8C141721" />
  <SolidColorBrush x:Key="GlassStrong" Color="#5E1B1F2A" />
  <SolidColorBrush x:Key="GlassInput" Color="#4A0D0F14" />
  <SolidColorBrush x:Key="GlassHover" Color="#8C2A2F3C" />
  <SolidColorBrush x:Key="Hairline" Color="#22FFFFFF" />
  <SolidColorBrush x:Key="HairlineStrong" Color="#38FFFFFF" />
  <!-- fallback window backdrop: dark ink with soft accent and cool glows -->
  <DrawingBrush x:Key="Backdrop" Stretch="Fill" TileMode="None">
    <DrawingBrush.Drawing>
      <DrawingGroup>
        <GeometryDrawing Geometry="M0,0 L520,0 L520,760 L0,760 Z">
          <GeometryDrawing.Brush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
              <GradientStop Color="#0E1016" Offset="0" />
              <GradientStop Color="#0A0B10" Offset="1" />
            </LinearGradientBrush>
          </GeometryDrawing.Brush>
        </GeometryDrawing>
        <GeometryDrawing Geometry="M-80,-120 A230,200 0 1 0 -79.9,-120 Z">
          <GeometryDrawing.Brush>
            <RadialGradientBrush>
              <GradientStop Color="#38FF7A00" Offset="0" />
              <GradientStop Color="#00FF7A00" Offset="1" />
            </RadialGradientBrush>
          </GeometryDrawing.Brush>
        </GeometryDrawing>
        <GeometryDrawing Geometry="M560,700 A260,220 0 1 0 559.9,700 Z">
          <GeometryDrawing.Brush>
            <RadialGradientBrush>
              <GradientStop Color="#2A3A6CFF" Offset="0" />
              <GradientStop Color="#003A6CFF" Offset="1" />
            </RadialGradientBrush>
          </GeometryDrawing.Brush>
        </GeometryDrawing>
      </DrawingGroup>
    </DrawingBrush.Drawing>
  </DrawingBrush>
  <DropShadowEffect x:Key="PanelShadow" BlurRadius="28" ShadowDepth="6" Direction="270" Opacity="0.45" Color="#000000" />
  <DropShadowEffect x:Key="AccentGlow" BlurRadius="22" ShadowDepth="0" Opacity="0.45" Color="#FF7A00" />

  <!-- ===== glass panel ===== -->
  <Style x:Key="GlassPanel" TargetType="{x:Type Border}">
    <Setter Property="Background" Value="{StaticResource Glass}" />
    <Setter Property="BorderBrush" Value="{StaticResource Hairline}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="18" />
    <Setter Property="Effect" Value="{StaticResource PanelShadow}" />
  </Style>
  <Style x:Key="GlassInset" TargetType="{x:Type Border}">
    <Setter Property="Background" Value="{StaticResource GlassInput}" />
    <Setter Property="BorderBrush" Value="{StaticResource Hairline}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="12" />
  </Style>

  <!-- ===== buttons ===== -->
  <Style x:Key="BigBtn" TargetType="{x:Type Button}">
    <Setter Property="Background" Value="{StaticResource GlassStrong}" />
    <Setter Property="Foreground" Value="{StaticResource Text}" />
    <Setter Property="BorderBrush" Value="{StaticResource Hairline}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="FrameworkElement.Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Button}">
          <Border Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="14" Padding="10">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="Background" Value="{StaticResource GlassHover}" />
              <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Accent}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="bd" Property="Background" Value="#40FF7A00" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{StaticResource TextFaint}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="GoBtn" TargetType="{x:Type Button}">
    <Setter Property="Background" Value="{StaticResource Accent}" />
    <Setter Property="Foreground" Value="{StaticResource Ink}" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="FrameworkElement.Cursor" Value="Hand" />
    <Setter Property="Effect" Value="{StaticResource AccentGlow}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Button}">
          <Border Name="bd" Background="{TemplateBinding Background}" CornerRadius="14" Padding="10">
            <Border.BorderBrush>
              <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#66FFFFFF" Offset="0" />
                <GradientStop Color="#00FFFFFF" Offset="0.6" />
              </LinearGradientBrush>
            </Border.BorderBrush>
            <Border.BorderThickness>1</Border.BorderThickness>
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="Background" Value="#FF8F2A" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="bd" Property="Background" Value="#E56D00" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="bd" Property="Background" Value="#4A3A2A18" />
              <Setter Property="Foreground" Value="#7A5A38" />
              <Setter Property="Effect" Value="{x:Null}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="MiniBtn" TargetType="{x:Type Button}">
    <Setter Property="Background" Value="{StaticResource GlassStrong}" />
    <Setter Property="Foreground" Value="{StaticResource Text}" />
    <Setter Property="FontSize" Value="10" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="FrameworkElement.Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Button}">
          <Border Name="bd" Background="{TemplateBinding Background}" BorderBrush="{StaticResource Hairline}" BorderThickness="1" CornerRadius="9" Padding="10,4,10,4">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="Background" Value="{StaticResource Accent}" />
              <Setter Property="Foreground" Value="{StaticResource Ink}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===== segmented pills ===== -->
  <Style x:Key="Seg" TargetType="{x:Type RadioButton}">
    <Setter Property="Foreground" Value="{StaticResource TextDim}" />
    <Setter Property="FontSize" Value="11" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="FrameworkElement.Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RadioButton}">
          <Border Name="pill" Background="#00000000" CornerRadius="14" Padding="0,7,0,7" Margin="3">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="pill" Property="Background" Value="{StaticResource Accent}" />
              <Setter TargetName="pill" Property="Effect" Value="{StaticResource AccentGlow}" />
              <Setter Property="Foreground" Value="{StaticResource Ink}" />
            </Trigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsChecked" Value="False" />
                <Condition Property="UIElement.IsMouseOver" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="pill" Property="Background" Value="{StaticResource GlassHover}" />
              <Setter Property="Foreground" Value="{StaticResource Text}" />
            </MultiTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="SegSmall" BasedOn="{StaticResource Seg}" TargetType="{x:Type RadioButton}">
    <Setter Property="FontSize" Value="10" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RadioButton}">
          <Border Name="pill" Background="#00000000" CornerRadius="10" Padding="0,4,0,4" Margin="2">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="pill" Property="Background" Value="{StaticResource Accent}" />
              <Setter Property="Foreground" Value="{StaticResource Ink}" />
            </Trigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsChecked" Value="False" />
                <Condition Property="UIElement.IsMouseOver" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="pill" Property="Background" Value="{StaticResource GlassHover}" />
              <Setter Property="Foreground" Value="{StaticResource Text}" />
            </MultiTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===== inputs ===== -->
  <Style x:Key="GlassTextBox" TargetType="{x:Type TextBox}">
    <Setter Property="Background" Value="{StaticResource GlassInput}" />
    <Setter Property="Foreground" Value="{StaticResource Text}" />
    <Setter Property="BorderBrush" Value="{StaticResource Hairline}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="10,7,10,7" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="CaretBrush" Value="{StaticResource Accent}" />
    <Setter Property="SelectionBrush" Value="{StaticResource Accent}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type TextBox}">
          <Border Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" Padding="{TemplateBinding Padding}">
            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Accent}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="GlassRich" TargetType="{x:Type RichTextBox}">
    <Setter Property="Background" Value="{StaticResource GlassInput}" />
    <Setter Property="Foreground" Value="{StaticResource Text}" />
    <Setter Property="BorderBrush" Value="{StaticResource Hairline}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="8,6,8,6" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RichTextBox}">
          <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12" Padding="{TemplateBinding Padding}">
            <ScrollViewer x:Name="PART_ContentHost" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="GlassListItem" TargetType="{x:Type ListBoxItem}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListBoxItem}">
          <Border Name="row" Background="#00000000" BorderBrush="#00000000" BorderThickness="3,0,0,0" Padding="12,7,12,7" Margin="0,1,0,1" CornerRadius="0,10,10,0">
            <ContentPresenter />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="True">
              <Setter TargetName="row" Property="Background" Value="#1AFFFFFF" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="row" Property="Background" Value="#26FF7A00" />
              <Setter TargetName="row" Property="BorderBrush" Value="{StaticResource Accent}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ===== slim scrollbar ===== -->
  <Style x:Key="SlimThumb" TargetType="{x:Type Thumb}">
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Border Name="b" Background="#26FFFFFF" CornerRadius="4" Margin="2" />
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="b" Property="Background" Value="#66FFFFFF" />
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
              <Setter TargetName="b" Property="Background" Value="{StaticResource Accent}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style TargetType="{x:Type ScrollBar}">
    <Setter Property="Background" Value="#00000000" />
    <Setter Property="Width" Value="10" />
    <Setter Property="MinWidth" Value="10" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ScrollBar}">
          <Grid Background="#00000000">
            <Track Name="PART_Track" IsDirectionReversed="True">
              <Track.DecreaseRepeatButton>
                <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False" />
              </Track.DecreaseRepeatButton>
              <Track.IncreaseRepeatButton>
                <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False" />
              </Track.IncreaseRepeatButton>
              <Track.Thumb>
                <Thumb Style="{StaticResource SlimThumb}" />
              </Track.Thumb>
            </Track>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="Orientation" Value="Horizontal">
        <Setter Property="Width" Value="Auto" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="Height" Value="10" />
        <Setter Property="MinHeight" Value="10" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ScrollBar}">
              <Grid Background="#00000000">
                <Track Name="PART_Track" IsDirectionReversed="False">
                  <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageLeftCommand" Opacity="0" Focusable="False" />
                  </Track.DecreaseRepeatButton>
                  <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageRightCommand" Opacity="0" Focusable="False" />
                  </Track.IncreaseRepeatButton>
                  <Track.Thumb>
                    <Thumb Style="{StaticResource SlimThumb}" />
                  </Track.Thumb>
                </Track>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- ===== window chrome: mac-style traffic lights (native caption buttons are removed by WindowChrome) ===== -->
  <StreamGeometry x:Key="GlyphClose">M0,0 L6,6 M6,0 L0,6</StreamGeometry>
  <StreamGeometry x:Key="GlyphMin">M0,3 L6,3</StreamGeometry>
  <StreamGeometry x:Key="GlyphMax">M0,3 L6,3 M3,0 L3,6</StreamGeometry>
  <Style x:Key="Traffic" TargetType="{x:Type Button}">
    <Setter Property="Width" Value="12" />
    <Setter Property="Height" Value="12" />
    <Setter Property="Margin" Value="0,0,8,0" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="FrameworkElement.Cursor" Value="Hand" />
    <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Button}">
          <Grid Background="#00000000">
            <Ellipse Name="dot" Fill="{TemplateBinding Background}" Stroke="#33000000" StrokeThickness="0.5" />
            <Path Name="glyph" Data="{TemplateBinding Tag}" Stroke="#A6000000" StrokeThickness="1.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Width="6" Height="6" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="True">
              <Setter TargetName="glyph" Property="Opacity" Value="1" />
            </Trigger>
            <Trigger Property="ButtonBase.IsPressed" Value="True">
              <Setter TargetName="dot" Property="Opacity" Value="0.7" />
            </Trigger>
            <Trigger Property="UIElement.IsEnabled" Value="False">
              <Setter TargetName="glyph" Property="Opacity" Value="0" />
              <Setter TargetName="dot" Property="Opacity" Value="0.35" />
              <Setter Property="FrameworkElement.Cursor" Value="Arrow" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="TrafficClose" BasedOn="{StaticResource Traffic}" TargetType="{x:Type Button}">
    <Setter Property="Background" Value="#FF5F57" />
    <Setter Property="Tag" Value="{StaticResource GlyphClose}" />
    <Setter Property="ToolTip" Value="Close" />
  </Style>
  <Style x:Key="TrafficMin" BasedOn="{StaticResource Traffic}" TargetType="{x:Type Button}">
    <Setter Property="Background" Value="#FEBC2E" />
    <Setter Property="Tag" Value="{StaticResource GlyphMin}" />
    <Setter Property="ToolTip" Value="Minimize" />
  </Style>
  <Style x:Key="TrafficMax" BasedOn="{StaticResource Traffic}" TargetType="{x:Type Button}">
    <Setter Property="Background" Value="#28C840" />
    <Setter Property="Tag" Value="{StaticResource GlyphMax}" />
    <Setter Property="IsEnabled" Value="False" />
  </Style>
</ResourceDictionary>
